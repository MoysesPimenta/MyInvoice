{
  "name": "myinvoice",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": ">=18 <=20"
  },
  "scripts": {
    "lint": "eslint \"**/*.{js,gs}\" --max-warnings 0 && stylelint \"**/*.css\" --max-warnings 0",
    "format": "prettier --write \"**/*.{js,gs,css,md}\""
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "prettier": "^3.2.5",
    "stylelint": "^16.2.1"
  }
}
